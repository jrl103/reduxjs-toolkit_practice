import { createSlice } from "@reduxjs/toolkit"; 

// Reducer
const practiceSlice = createSlice({ 
    name: "practice", 
    initialState: { 

    }, 
    reducers: {

     }, 
    }); 
    
    
    
const { } = practiceSlice.actions;
 export default practiceSlice.reducer;
